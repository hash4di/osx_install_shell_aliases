# osx_install_shell_aliases

Collection of shell aliases to install Mac OS X environment on clean
machine.

### Installation

bash -c "$(curl -fsSL 'https://raw.github.com/hash4di/osx_install_shell_aliases/master/setup_macbook-pro.sh')"